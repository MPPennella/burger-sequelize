<h1>Burgers!</h1>

<h2>Burgers that are not eaten</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
        {{> burgers/burgerItem devoured=false}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
        {{> burgers/burgerItem devoured=true}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="newBurgerName">Burger Name:</label>
    <input type="text" id="newBurgerName" name="name">
  </div>

  <div class="form-group">
    <label>Has burger been eaten?</label><br>
    <input type="radio" name="eaten" value="1" checked> Yes<br>
    <input type="radio" name="eaten" value="0"> No
  </div>

  <button type="submit">Add Burger</button>
</form>
